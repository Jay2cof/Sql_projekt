create database result;
use result;
Create Table subjects(
score Int,
grade varchar(9),
course varchar(100),
subject_name varchar (50)

);
select * from subjects;
insert into subjects values (30, 'Fail', 'Chemistry','sam');
Drop table 
subjects;

